function retorna(){
    if(true > false){ 
        const frase = 'HTML5 | CSS3 | JAVASCRIPT | GIT | GITHUB | NODEJS | MONGODB'
        const minusculo = frase.toLowerCase();
        return minusculo;
    };
}
retorna();
console.log(retorna())